import React from 'react';
import { fetchCoverArt } from '../services/api-call';

const Releases = () => {
  fetchCoverArt();
  // const mappedReleases = releases.map(release => {
    
  // });
  return (
    <p>thing</p>
  );
};

export default Releases;
